

现在已经能看到一个可视化效果了，但先说清楚状态：

现在已经补成一键脚本了，常用入口都在工作区的 `scripts/` 目录里。

最常用的是这个：


```
bash scripts/run_moveit_mock_demo.sh
```

其它脚本：

```bash
bash scripts/build.sh
bash scripts/run_mock_perception.sh
bash scripts/run_moveit_demo.sh
bash scripts/run_planning_server.sh
```


``` bash
bash scripts/run_moveit_mock_demo.sh rviz:=true use_ros2_control:=true clutter_count:=0 publish_target_box_points:=false publish_clutter_points:=false publish_floor_points:=false task_auto_start_on_target:=true task_auto_accept_base_arrival:=true

```


一个专门调姿态的脚本，只开：

机器人
货架点云
MoveIt Plan / Execute
不启任务状态机
直接运行：
```bash
bash scripts/run_moveit_shelf_debug.sh
```
脚本在这里：
run_moveit_shelf_debug.sh

新版进展可视化”接进去了，你现在可以直接用：
```bash
bash scripts/run_moveit_mock_progress.sh
```
